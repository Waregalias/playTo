# Map 3D Tiles Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer le rendu 2D plat des hexagones de la carte par des tuiles hexagonales en relief vues 3/4 du dessus (vecteur extrudé), sans toucher au flux de données, à l'adjacence, au panneau ni aux interactions.

**Architecture :** Toute la géométrie (projection 3/4, extrusion des parois, ombre, tri de profondeur, teintes) est extraite dans un module **pur** `map-geometry.ts` — fonctions sans dépendance Angular, testables au niveau unité. Le composant `map-screen.ts` ne fait que consommer ces fonctions dans ses `computed`. Le template SVG dessine, par tuile et de l'arrière vers l'avant, un groupe `ombre → paroi gauche → paroi droite → face du dessus`. Le SCSS style les faces/parois/ombre et conserve les animations existantes.

**Tech Stack :** Angular 22 (standalone, signals, `@if/@for`), SVG inline, SCSS de composant, Vitest pour les tests web, monorepo pnpm.

## Global Constraints

- **Périmètre strict :** ne modifier QUE le rendu de la carte. Aucun changement à l'API, au store, à l'adjacence, au panneau `hex-panel`, ni aux actions `move`/`rest`/`search`.
- **Coordonnées & logique serveur intactes :** les axiales `(q, r)` et `isAdjacent` (package `shared`) ne changent pas. Seule la **projection écran** change ; les voisins restent jointifs (une mise à l'échelle verticale uniforme préserve la tesselation).
- **`data-testid` préservés :** chaque tuile garde `data-testid="hex-{q}-{r}"` (déplacé sur le `<g>`), le clic reste fonctionnel (bulle). `hex-panel`, `move-btn`, `rest-btn`, `search-btn` inchangés.
- **Tokens DESIGN.md** (`apps/web/src/styles.scss`) : `--g-ember-glow: #ffc46b`, `--g-border: #2c3b4b`, `--g-mist: #7e93a6`, `--g-bone: #e9dfc9`, `--g-panel: #17212c`. Couleurs de terrain (`TERRAIN_FILLS`) inchangées ; parois et ombres en sont **dérivées** (assombrissement) — aucune couleur chaude nouvelle.
- **Relief SUBTIL, pointy-top** (décisions de spec) : parois basses, inclinaison légère.
- **`prefers-reduced-motion: reduce`** : animations décoratives coupées (ne pas régresser). Zones tactiles ≥ 44px, `role="img"` + `aria-label` conservés.
- **Aucune chaîne FR en dur**, code 100 % anglais (identifiants, commentaires).
- **Aucun commentaire « claude »/« ai »** dans le code.
- **Définition de « terminé » :** `pnpm --filter web lint && pnpm --filter web build` passent, tests unitaires verts, rendu conforme à la spec vérifié en preview (1280px et 375px), aucune tuile rognée aux bords du viewBox.

### Coordination avec le plan responsive (`2026-07-10-web-responsive.md`)

Les 3 fichiers de la carte sont partagés avec les tâches A3 et B6 de ce plan. **L'ordre d'exécution entre les deux plans est libre** car les régions touchées sont disjointes :

- **A3** (responsive) : enveloppe le titre dans `<header class="maphead">` (lignes 1-2) + grille CSS wide, dont `.mapwrap svg { max-height: calc(100dvh - 220px); width: auto; }`. **Ne touche pas** `projectCenter`/`bounds`/l'intérieur du `<svg>`.
- **B6** (vignette) : ajoute un `computed terrainVignette` (ajout en fin de classe) + `<img class="vignette">` dans le panneau + `.vignette` en SCSS. **Ne touche pas** le rendu SVG des tuiles.
- **Ce plan** : réécrit l'intérieur du `<svg>` (lignes 5-41) + `center`/`corners`/`HexView`/`hexViews`/`bounds` en TS + styles `.hex`→`.tile*` en SCSS.

**Règles de non-collision à respecter en exécutant CE plan :**
1. **Ne pas** introduire ni supprimer `<header class="maphead">` — laisser les lignes de titre exactement telles qu'elles sont à l'ouverture du fichier (que A3 soit passé ou non).
2. **Ne pas** toucher au bloc `<div class="card panel">` ni à un éventuel `<img class="vignette">`.
3. Le viewBox produit reste à ratio proportionnel : sous la contrainte `max-height … ; width:auto` de A3, le SVG se met à l'échelle sans déformation — vérifié en tâche 2.

---

## File Structure

**Créés :**
- `apps/web/src/app/game/map/map-geometry.ts` — module **pur** : constantes de rendu, projection 3/4, extrusion, ombre, teintes, tri de profondeur, bornes. Une seule responsabilité : transformer des hexes de données en primitives de dessin.
- `apps/web/src/app/game/map/map-geometry.spec.ts` — tests unitaires Vitest du module pur.

**Modifiés :**
- `apps/web/src/app/game/map/map-screen.ts` — consomme `map-geometry` ; retire les helpers de géométrie inline (`center`, `corners`, `HexView`, constantes, `TERRAIN_FILLS`).
- `apps/web/src/app/game/map/map-screen.html` — réécrit l'intérieur du `<svg>` (blocs 3D triés + passes overlay).
- `apps/web/src/app/game/map/map-screen.scss` — remplace les styles `.hex`/`.hexlabel` par `.tile`/`.tile-top`/`.tile-wall`/`.tile-shadow` ; conserve mist/pulse/reduced-motion et le reste.

---

# Task 1 : Module de géométrie pur + tests (TDD)

**Files:**
- Create: `apps/web/src/app/game/map/map-geometry.ts`
- Test: `apps/web/src/app/game/map/map-geometry.spec.ts`

**Interfaces:**
- Produces (importés par Task 2) :
  - `TILT: number`, `DEPTH: number` — constantes de rendu.
  - `interface TileView { hex: HexDto; x: number; y: number; topPoints: string; wallLeft: string; wallRight: string; topFill: string; wallLeftFill: string; wallRightFill: string; shadowCx: number; shadowCy: number; shadowRx: number; shadowRy: number; }`
  - `projectCenter(q: number, r: number): { x: number; y: number }` — centre projeté (y compressé par `TILT`).
  - `shade(hex: string, factor: number): string` — assombrit une couleur `#rrggbb`.
  - `toTileView(hex: HexDto): TileView` — construit les primitives de dessin d'une tuile.
  - `byDepth(a: TileView, b: TileView): number` — comparateur de tri arrière→avant (r puis q).
  - `boundsOf(hexes: readonly HexDto[]): { minX: number; minY: number; width: number; height: number }` — bornes du viewBox (marge + extrusion).

- [ ] **Step 1 : Écrire les tests (échouants)**

Créer `apps/web/src/app/game/map/map-geometry.spec.ts` :

```ts
import { describe, expect, it } from 'vitest';
import type { HexDto } from '@aldenfer/shared';
import {
  boundsOf,
  byDepth,
  DEPTH,
  projectCenter,
  shade,
  TILT,
  toTileView,
} from './map-geometry';

function hex(partial: Partial<HexDto> & Pick<HexDto, 'q' | 'r'>): HexDto {
  return {
    id: `${partial.q}:${partial.r}`,
    q: partial.q,
    r: partial.r,
    discovered: true,
    terrain: 'plain',
    poi: null,
    mistLevel: 0,
    ...partial,
  } as HexDto;
}

describe('map-geometry', () => {
  it('compresses the vertical axis by TILT, leaves x untouched', () => {
    const p = projectCenter(0, 2);
    // x uses the standard pointy-top formula, unaffected by TILT
    expect(p.x).toBeCloseTo(22 * Math.sqrt(3), 3);
    // y = 22 * 1.5 * 2 * TILT
    expect(p.y).toBeCloseTo(22 * 1.5 * 2 * TILT, 3);
    expect(TILT).toBeLessThan(1);
  });

  it('darkens a colour channel by the factor', () => {
    expect(shade('#8e9c6b', 0.5)).toBe('#474e36');
    expect(shade('#ffffff', 1)).toBe('#ffffff');
    expect(shade('#ffffff', 0)).toBe('#000000');
  });

  it('builds a hex top face (6 points) and two wall skirts (4 points each)', () => {
    const view = toTileView(hex({ q: 0, r: 0 }));
    expect(view.topPoints.split(' ')).toHaveLength(6);
    expect(view.wallLeft.split(' ')).toHaveLength(4);
    expect(view.wallRight.split(' ')).toHaveLength(4);
    // walls are darker than the top face
    expect(view.wallLeftFill).not.toBe(view.topFill);
    expect(view.wallRightFill).not.toBe(view.topFill);
  });

  it('uses the fog fill and derived walls for undiscovered hexes', () => {
    const view = toTileView(hex({ q: 1, r: 1, discovered: false }));
    expect(view.topFill).toBe('#26313d');
  });

  it('orders tiles back-to-front by row then column', () => {
    const tiles = [hex({ q: 2, r: 1 }), hex({ q: 0, r: 0 }), hex({ q: 1, r: 0 })]
      .map(toTileView)
      .sort(byDepth);
    expect(tiles.map((t) => `${t.hex.q}:${t.hex.r}`)).toEqual(['0:0', '1:0', '2:1']);
  });

  it('returns a default box for an empty map', () => {
    expect(boundsOf([])).toEqual({ minX: 0, minY: 0, width: 340, height: 300 });
  });

  it('reserves margin and extrusion room in the bounds of a single hex', () => {
    // single hex at projected (0,0): min=max=0 → width = 2*MARGIN(30), height = 2*MARGIN + DEPTH
    const b = boundsOf([hex({ q: 0, r: 0 })]);
    expect(b.width).toBe(60);
    expect(b.height).toBe(60 + DEPTH);
  });
});
```

- [ ] **Step 2 : Lancer les tests → échec**

Run : `pnpm --filter web test -- map-geometry`
Expected : FAIL (`map-geometry` introuvable).

- [ ] **Step 3 : Implémenter le module pur**

Créer `apps/web/src/app/game/map/map-geometry.ts` :

```ts
import type { HexDto, Terrain } from '@aldenfer/shared';

/**
 * Pure rendering geometry for the hex map: a 3/4 top-down projection with
 * extruded tile blocks. No Angular dependency so it can be unit-tested in
 * isolation. The screen projection is the only thing that changes versus the
 * flat map; axial coordinates and adjacency (package `shared`) are untouched.
 */

const HEX_RADIUS = 22; // viewBox units, pointy-top (SPEC-M1 / maquette)
const SQRT3 = Math.sqrt(3);
const MARGIN = 30;

/** Vertical squish that fakes the 3/4 viewing tilt (subtle relief). */
export const TILT = 0.62;
/** Downward extrusion of the block walls, in viewBox units (subtle). */
export const DEPTH = 10;

/** Fog fill for undiscovered hexes (matches the previous flat rendering). */
const FOG_FILL = '#26313d';
/** Side-wall darkening, light read as coming from the upper-right. */
const WALL_RIGHT = 0.62; // lit facet
const WALL_LEFT = 0.45; // shadowed facet

// Terrain fills from DESIGN §4 (ford: cold water tone, same muted range).
const TERRAIN_FILLS: Record<Terrain, string> = {
  plain: '#8e9c6b',
  forest: '#4f6b4a',
  hill: '#8a7b5c',
  marsh: '#5e6e63',
  ruins: '#6e6a72',
  ash_road: '#3d4854',
  ford: '#4a6272',
  shrine: '#b0885a',
};

export interface TileView {
  hex: HexDto;
  x: number; // top-face centre x
  y: number; // top-face centre y
  topPoints: string;
  wallLeft: string;
  wallRight: string;
  topFill: string;
  wallLeftFill: string;
  wallRightFill: string;
  shadowCx: number;
  shadowCy: number;
  shadowRx: number;
  shadowRy: number;
}

export function projectCenter(q: number, r: number): { x: number; y: number } {
  return {
    x: HEX_RADIUS * (SQRT3 * q + (SQRT3 / 2) * r),
    y: HEX_RADIUS * 1.5 * r * TILT,
  };
}

export function shade(hex: string, factor: number): string {
  const n = parseInt(hex.slice(1), 16);
  const r = Math.min(255, Math.round(((n >> 16) & 255) * factor));
  const g = Math.min(255, Math.round(((n >> 8) & 255) * factor));
  const b = Math.min(255, Math.round((n & 255) * factor));
  return `#${((r << 16) | (g << 8) | b).toString(16).padStart(6, '0')}`;
}

type Point = { x: number; y: number };

function tiltedCorners(cx: number, cy: number): Point[] {
  return Array.from({ length: 6 }, (_, i) => {
    const angle = (Math.PI / 180) * (60 * i - 30);
    return {
      x: cx + HEX_RADIUS * Math.cos(angle),
      y: cy + HEX_RADIUS * Math.sin(angle) * TILT,
    };
  });
}

function toPoints(points: Point[]): string {
  return points.map((p) => `${p.x.toFixed(1)},${p.y.toFixed(1)}`).join(' ');
}

export function toTileView(hex: HexDto): TileView {
  const { x, y } = projectCenter(hex.q, hex.r);
  const c = tiltedCorners(x, y);
  const down = (p: Point): Point => ({ x: p.x, y: p.y + DEPTH });
  const topFill = hex.discovered ? TERRAIN_FILLS[hex.terrain] : FOG_FILL;

  // Corner order (from tiltedCorners): 0=upper-right, 1=lower-right, 2=bottom,
  // 3=lower-left, 4=upper-left, 5=top. The two front-facing lower edges become
  // the visible side walls once extruded downward.
  return {
    hex,
    x,
    y,
    topPoints: toPoints(c),
    wallRight: toPoints([c[1], c[2], down(c[2]), down(c[1])]),
    wallLeft: toPoints([c[2], c[3], down(c[3]), down(c[2])]),
    topFill,
    wallRightFill: shade(topFill, WALL_RIGHT),
    wallLeftFill: shade(topFill, WALL_LEFT),
    shadowCx: x,
    shadowCy: y + DEPTH + 3,
    shadowRx: HEX_RADIUS * 0.82,
    shadowRy: HEX_RADIUS * 0.3,
  };
}

/** Painter's-algorithm order: far rows first, then columns for determinism. */
export function byDepth(a: TileView, b: TileView): number {
  return a.hex.r - b.hex.r || a.hex.q - b.hex.q;
}

export function boundsOf(
  hexes: readonly HexDto[],
): { minX: number; minY: number; width: number; height: number } {
  if (hexes.length === 0) return { minX: 0, minY: 0, width: 340, height: 300 };
  let minX = Infinity;
  let minY = Infinity;
  let maxX = -Infinity;
  let maxY = -Infinity;
  for (const hex of hexes) {
    const { x, y } = projectCenter(hex.q, hex.r);
    minX = Math.min(minX, x);
    maxX = Math.max(maxX, x);
    minY = Math.min(minY, y);
    maxY = Math.max(maxY, y);
  }
  maxY += DEPTH; // room for the extruded walls + contact shadow of the front row
  return {
    minX: minX - MARGIN,
    minY: minY - MARGIN,
    width: maxX - minX + 2 * MARGIN,
    height: maxY - minY + 2 * MARGIN,
  };
}
```

> Vérifier que `Terrain` et `HexDto` sont bien exportés par `@aldenfer/shared` (ils le sont : déjà importés dans `map-screen.ts`). Si `HexDto` a des champs supplémentaires requis, le helper `hex()` du test devra les fournir — adapter le cast au besoin.

- [ ] **Step 4 : Lancer les tests → succès**

Run : `pnpm --filter web test -- map-geometry`
Expected : PASS (7 tests).

- [ ] **Step 5 : Vérifier lint + build**

Run : `pnpm --filter web lint && pnpm --filter web build`
Expected : OK (le nouveau module n'est pas encore importé, mais doit compiler/linter proprement).

- [ ] **Step 6 : Commit**

```bash
git -C /Volumes/macOS/Tech/playTo add apps/web/src/app/game/map/map-geometry.ts apps/web/src/app/game/map/map-geometry.spec.ts
git -C /Volumes/macOS/Tech/playTo commit -m "feat(web): pure 3/4 hex tile geometry module with tests"
```

---

# Task 2 : Câbler le rendu 3D dans le composant carte

**Files:**
- Modify: `apps/web/src/app/game/map/map-screen.ts`
- Modify: `apps/web/src/app/game/map/map-screen.html`
- Modify: `apps/web/src/app/game/map/map-screen.scss`

**Interfaces:**
- Consumes: `TileView`, `boundsOf`, `byDepth`, `toTileView` (Task 1).

- [ ] **Step 1 : Remplacer la géométrie inline du composant par les imports du module**

Dans `apps/web/src/app/game/map/map-screen.ts` :

Remplacer le bloc d'imports/constantes/helpers en tête. **Supprimer** les constantes `HEX_RADIUS`, `SQRT3`, `MARGIN`, la map `TERRAIN_FILLS`, l'interface `HexView`, et les fonctions `center()` et `corners()` (lignes ~15-51). À la place, ajouter l'import du module de géométrie sous les imports existants :

```ts
import { boundsOf, byDepth, toTileView, type TileView } from './map-geometry';
```

Le haut du fichier doit donc ressembler à :

```ts
import { Component, computed, inject, signal } from '@angular/core';
import {
  isAdjacent,
  moveCost,
  movePayloadSchema,
  type HexDto,
  type MistLevel,
  type Terrain,
} from '@aldenfer/shared';
import { REGION_NAMES_FR, UI_FR, ERROR_MESSAGES_FR } from '@aldenfer/shared/content/fr';
import { ApiClient, ApiError } from '../../core/api-client';
import { GameStore } from '../../core/game-store';
import { ToastService } from '../../core/toast';
import { boundsOf, byDepth, toTileView, type TileView } from './map-geometry';
```

> `Terrain` n'est plus utilisé après suppression de `TERRAIN_FILLS` : retirer `type Terrain,` de l'import `@aldenfer/shared` **si** aucun autre usage ne subsiste (le linter le signalera — `MistLevel` reste utilisé par `moveCost`).

- [ ] **Step 2 : Réécrire `hexViews` et `bounds` avec le module**

Toujours dans `map-screen.ts`, remplacer les `computed` `bounds` et `hexViews` (lignes ~89-122) par :

```ts
  private readonly bounds = computed(() => boundsOf(this.store.hexes()));

  readonly viewBox = computed(() => {
    const b = this.bounds();
    return `${b.minX} ${b.minY} ${b.width} ${b.height}`;
  });

  readonly hexViews = computed<TileView[]>(() =>
    this.store.hexes().map(toTileView).sort(byDepth),
  );
```

> `playerView`, `selectedView`, `panel` et les méthodes d'action restent inchangés : ils n'utilisent que `view.hex.*` (et `.x/.y` pour le marqueur), tous présents sur `TileView`.

- [ ] **Step 3 : Réécrire l'intérieur du `<svg>` (blocs 3D triés + overlays)**

Dans `apps/web/src/app/game/map/map-screen.html`, remplacer **tout le bloc `<svg> … </svg>`** (lignes ~5-41) par le suivant. **Ne pas** toucher aux lignes de titre au-dessus (`eyebrow`/`h2` ou `<header class="maphead">` si A3 est passé) ni au `<div class="card panel">` en dessous :

```html
  <svg
    [attr.viewBox]="viewBox()"
    role="img"
    [attr.aria-label]="'Carte — ' + regionName()"
  >
    <!-- 3D tile blocks, painted back-to-front (see byDepth) -->
    @for (view of hexViews(); track view.hex.id) {
      <g
        class="tile"
        [class.sel]="view.hex.id === selectedId()"
        (click)="select(view.hex.id)"
        [attr.data-testid]="'hex-' + view.hex.q + '-' + view.hex.r"
      >
        <ellipse
          class="tile-shadow"
          [attr.cx]="view.shadowCx"
          [attr.cy]="view.shadowCy"
          [attr.rx]="view.shadowRx"
          [attr.ry]="view.shadowRy"
        />
        <polygon class="tile-wall" [attr.points]="view.wallLeft" [attr.fill]="view.wallLeftFill" />
        <polygon class="tile-wall" [attr.points]="view.wallRight" [attr.fill]="view.wallRightFill" />
        <polygon class="tile-top" [attr.points]="view.topPoints" [attr.fill]="view.topFill" />
      </g>
    }
    <!-- mist drift over fog hexes -->
    @for (view of hexViews(); track view.hex.id) {
      @if (!view.hex.discovered) {
        <ellipse class="mist-drift" [attr.cx]="view.x" [attr.cy]="view.y - 4" rx="16" ry="6" fill="#7E93A6" />
        <ellipse class="mist-drift slow" [attr.cx]="view.x - 5" [attr.cy]="view.y + 6" rx="13" ry="5" fill="#5A6B7A" />
      }
    }
    <!-- POI / shrine glyphs on the top face -->
    @for (view of hexViews(); track view.hex.id) {
      @if (view.hex.discovered && view.hex.poi) {
        <text class="hexlabel" [attr.x]="view.x" [attr.y]="view.y + 3">
          {{ view.hex.terrain === 'shrine' ? '⌂' : '✦' }}
        </text>
      }
    }
    <!-- player marker -->
    @if (playerView(); as player) {
      <circle [attr.cx]="player.x" [attr.cy]="player.y" r="7" fill="#FFC46B" stroke="#1A0E02" stroke-width="2" />
      <circle class="pulse" [attr.cx]="player.x" [attr.cy]="player.y" r="11" fill="none" stroke="#FFC46B" stroke-width="1" opacity="0.5" />
    }
  </svg>
```

- [ ] **Step 4 : Remplacer les styles `.hex` par les styles de tuile 3D**

Dans `apps/web/src/app/game/map/map-screen.scss`, remplacer le bloc `.hex { … }` **et** le bloc `.hexlabel { … }` (lignes ~41-57) par :

```scss
.tile {
  cursor: pointer;
  transition: filter 0.15s;

  &:hover { filter: brightness(1.2); }
  &.sel .tile-top { stroke: var(--g-ember-glow); stroke-width: 2.5; }
}

.tile-top {
  stroke: #0a1119;
  stroke-width: 1;
}

.tile-wall {
  stroke: none;
}

.tile-shadow {
  fill: #05090d;
  opacity: 0.35;
  pointer-events: none;
}

.hexlabel {
  font-size: 9px;
  fill: #0b1218;
  font-weight: 700;
  pointer-events: none;
  text-anchor: middle;
}
```

> Conserver tel quel le reste du fichier : `.mapwrap`, `svg`, `.mist-drift`, `.pulse`, le bloc `@media (prefers-reduced-motion: reduce)`, `.card`, `.panel`, `.costs`, `.btn`, et (si A3/B6 sont passés) les blocs `@include layout.wide` et `.vignette`.

- [ ] **Step 5 : Vérifier lint + build**

Run : `pnpm --filter web lint && pnpm --filter web build`
Expected : OK. Si le linter signale `Terrain` inutilisé, retirer l'import résiduel (Step 1).

- [ ] **Step 6 : Vérifier visuellement (preview)**

Lancer le serveur de dev (outil preview) et ouvrir l'écran Carte avec un personnage connecté.

Vérifier à **375px** puis **1280px** :
- Les tuiles apparaissent en relief 3/4 : face du dessus colorée par terrain + deux parois latérales (droite plus claire, gauche plus sombre) + ombre douce sous chaque bloc.
- **Empilement correct** : une tuile proche (rangée du bas) recouvre proprement la paroi de la tuile derrière elle ; aucune paroi ne « traverse » une tuile de devant.
- **Aucun rognage** : les blocs des rangées du bas et leurs ombres tiennent dans le cadre `.mapwrap` (viewBox élargi par `boundsOf`).
- **Sélection** : cliquer un hex met un liseré braise sur sa face du dessus ; le panneau d'action affiche coût/temps/bouton pour un hex adjacent (comportement inchangé).
- **Brouillard** : les hexes non découverts sont des blocs sombres avec la brume animée qui flotte au-dessus.
- **Joueur & POI** : marqueur braise et glyphes `✦`/`⌂` posés au centre de la face du dessus.
- À **1280px** (si A3 est appliqué) : la carte tient dans la grille, le SVG se met à l'échelle sous `max-height` sans déformation (ratio conservé).
- **`prefers-reduced-motion`** (émuler « reduce » dans le navigateur) : brume et pulsation figées, relief 3D intact.

Prendre une capture (1280px) pour la revue. Expected : conforme à la spec ; aucune régression d'interaction.

- [ ] **Step 7 : Commit**

```bash
git -C /Volumes/macOS/Tech/playTo add apps/web/src/app/game/map/map-screen.ts apps/web/src/app/game/map/map-screen.html apps/web/src/app/game/map/map-screen.scss
git -C /Volumes/macOS/Tech/playTo commit -m "feat(web): render map hexes as extruded 3/4-view 3D tiles"
```

---

# Vérification finale

- [ ] **Lint + build web**

Run : `pnpm --filter web lint && pnpm --filter web build`
Expected : OK.

- [ ] **Tests web**

Run : `pnpm --filter web test -- map-geometry`
Expected : verts.

- [ ] **Revue visuelle**

Preview écran Carte à **1280px** et **375px** : relief 3/4 subtil, empilement correct, palette DESIGN respectée, interactions (sélection/déplacement/repos/fouille) intactes, aucun asset/tuile cassé, `prefers-reduced-motion` respecté.

---

## Notes d'exécution

- **Ordre :** Task 1 avant Task 2 (le composant consomme le module).
- **Indépendance des plans :** ce plan et le plan responsive (A3/B6) peuvent être exécutés dans n'importe quel ordre ; respecter les 3 règles de non-collision de la section « Coordination ».
- **Hors périmètre :** textures peintes sur les tuiles (`lands/*.png` = scènes verticales encadrées, inadaptées), zoom/pan, orientation flat-top, toute modification hors `apps/web/src/app/game/map/`.
